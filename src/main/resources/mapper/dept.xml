<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="deptdto">
<select id="Dept_getList" parameterType="DeptDto" resultType="DeptDto">
	select * from dept_tb;
</select>
</mapper>